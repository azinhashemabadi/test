services:
  web:
    build: .
    volumes:
      - .:/app
    expose:
      - "8000"
    env_file:
      - .env


  nginx:
    image: nginx:latest
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
      - .:/app
    ports:
      - "8000:80"
    depends_on:
      - web

